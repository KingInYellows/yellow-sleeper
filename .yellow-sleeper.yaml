# Live league identity is not stored in this repository.
# Copy `.yellow-sleeper.yaml.example` to this path (or set YELLOW_SLEEPER_CONFIG)
# and fill in your own values. The example file is synthetic only.
